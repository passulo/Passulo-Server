database {
  profile = "slick.jdbc.H2Profile$"
  dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
  connectionPool = disabled
  properties = {
    driver = org.h2.Driver
    url = "jdbc:h2:mem:passulo;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS passulo"
  }
}
